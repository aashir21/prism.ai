import React from 'react'

const SectionTitle = ({ title }: { title: string }) => {
    return (
        <h1 className='text-bold text-lg text-[#a3e635]'>{title}</h1>
    )
}

export default SectionTitle